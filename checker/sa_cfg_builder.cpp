#include "sa_cfg_builder.h"
#include <sys/debugging/debug.h>

namespace checklib
{


std::shared_ptr<sa_cfg> sa_cfg_builder::build(const sys::string& path) const
{
	ASSERT(!"KTTODO impl");
	std::shared_ptr<sa_cfg> cfg = std::make_shared<sa_cfg>();

	// Fill

	return cfg;
}


}
